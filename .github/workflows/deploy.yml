name: Deploy to Hostinger via FTP

on:
  push:
    branches:
      - master
  workflow_dispatch:  # Allows manual triggering

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Next.js project (Static Export)
        run: npm run build
        # This creates a static export in the 'out/' directory
        # Perfect for client-side only hosting (no server-side rendering)
      
      - name: Verify static export
        run: |
          echo "Checking build output..."
          ls -la out/ | head -20
      
      - name: Deploy static files to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          # IMPORTANT: FTP_SERVER should contain ONLY hostname or IP (no ftp://, no spaces, no port)
          # Examples: "ftp.yourdomain.com", "files.000webhostapp.com", or "147.93.109.155"
          server: ${{ secrets.FTP_SERVER }}
          port: 21
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          local-dir: ./out/
          server-dir: /public_html/
          # Exclude build artifacts and unnecessary files
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.next/**
            **/cache/**
            **/*.map
            **/trace

